apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: midstall-ingress
  namespace: midstall
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ldap.midstall.com
      secretName: tls-ldap.midstall.com
  rules:
    - host: 'midstall.com'
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: midstall-website
                port:
                  number: 80
    - host: 'cloud.midstall.com'
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: midstall-nextcloud
                port:
                  number: 80
    - host: 'ldap.midstall.com'
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: midstall-ldap-manager
                port:
                  number: 80